# 安全政策

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。不要在公开 Issue、Discussion、Pull Request 或 CI 日志中粘贴个人信息、可用凭据、内部地址或尚未修复的利用细节。

如果仓库尚未启用私密漏洞报告，请通过维护者的 GitHub 主页请求一个私密联系方式，并只发送不含秘密的概要。凭据一旦进入公开渠道，应立即在对应平台轮换；删除评论或提交不能使它重新安全。

建议报告包含：

- 受影响的 commit 或版本；
- 最小复现步骤；
- 预期与实际行为；
- 潜在影响；
- 不含真实秘密的概念验证。

维护者会尽快确认报告、评估影响，并在可行时协调修复与披露。此项目由志愿者维护，不承诺固定响应时限。

## 支持范围

安全修复只针对默认分支的最新版本。模板生成的个人简历内容、第三方字体、Overleaf 服务和本地 TeX 发行版本不属于本仓库的安全支持范围。

## 构建安全

- 只编译可信来源的 `.tex` 文件。TeX 可以读取本机文件，恶意模板不应被视为普通文档。
- 仓库脚本显式禁用 shell escape；不要为了第三方宏包移除此限制。
- CI 使用最小只读权限，checkout 不保留写入凭据，也不上传含个人内容的 PDF artifact。
- 不要在公开 CI 中编译真实简历，因为日志、缓存、artifact 和第三方 action 都可能扩大暴露面。
- 发布前运行 `make privacy-history`，并按 `docs/PRIVACY.md` 人工复核。

## 隐私扫描器的安全边界

`scripts/privacy_check.py` 用于发现常见误提交，不是秘密管理器、恶意软件扫描器或完整的数据防泄漏系统。它无法可靠识别人名、未公开项目代号、合同限制和经过编码或拆分的秘密。发现扫描绕过方式时，请按漏洞流程私下报告，不要提交含真实秘密的测试样本。
